import { User } from '../entities/user.entity';

export class CreateUserDto extends User {
  // email: string;
  // firstName: string;
  // lastName: string;
  // image: string;
  // pdf: Uint8Array;
}
